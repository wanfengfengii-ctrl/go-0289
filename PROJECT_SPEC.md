# 环境脱氧核糖核酸扩增批次污染判定闭环

## 项目目标

建设一个可编译运行并带真实操作页的 Go Web 服务，围绕一次环境脱氧核糖核酸扩增批次完成板位设计锁定、样本与阴阳性对照装载、仪器曲线分片接收、阈值循环数计算、污染传播、复孔复验和批次裁定。所有数值使用定点整数；证据以事务事件日志和快照持久化，重启后恢复上传游标、判读结果、复验代次、幂等响应与终局。生产后端规划 20—28 个 Go 文件、约 2500 行有效生产 Go 代码，严格保持 2000—3000 行，分布于至少 5 个有实际职责的包；测试、前端、注释和生成物不计入该规模。前端通过真实 HTTP API 提供板位锁定、分片上传、孔位判读、污染闭包、复验和终局操作。

## 端到端业务流程

1. 建立实验批次：录入采样批次、靶标、96 孔板布局、样本复孔、提取空白、无模板对照、阳性对照、试剂批号、阈值和相邻孔传播规则；锁定时生成不可变规则摘要与代次 1。
2. 装载与接收曲线：扫描样本管至指定孔位，校验唯一性和对照数量；仪器按运行号、孔位与连续分片序号上传整数荧光曲线，同一内容可安全重试，缺片或冲突片不得进入判读。
3. 确定性判读：曲线完整后执行基线扣除、连续越阈窗口和阈值循环数插值；阳性对照必须命中规定范围，所有阴性对照必须保持阴性，否则进入污染分析。
4. 污染传播与复验：从异常对照、可疑高浓度孔和装载冲突生成种子，沿锁定的同移液批次、相邻孔及共享提取批次边求最小闭包；以比较交换创建唯一下一代次，仅重开闭包内孔位。
5. 批次裁定：当前代次曲线完整、对照有效、样本复孔一致且复验闭合后，两名不同资格人员复核同一证据摘要；放行、隔离或作废竞争持久化单写屏障，首个合法结果成为终局。
6. 故障恢复：分片写入、判读、复验创建和终局提交均有事务边界；启动时校验日志并从快照重放，忽略未提交尾部，恢复所有可见状态和确定性响应。

## 核心组件与职责

1. 实验规则与板位锁定：维护靶标阈值、对照要求、复孔关系、试剂约束和传播图，校验板位完整性并生成内容摘要。
2. 样本装载与曲线接收：管理管码占用、仪器运行、连续分片、上传游标、幂等记录及曲线完整性。
3. 定点判读与质量门控：执行整数基线计算、连续越阈识别、阈值循环数插值、对照判读和复孔一致性检查。
4. 污染闭包与复验代次：根据公开边集生成排序后的最小影响集合，以代次隔离旧证据并控制并发复验创建。
5. 事件存储与终局仲裁：提供校验和日志、原子快照、事务提交、恢复重放、双人复核和单写终局。
6. HTTP API 与嵌入式前端：提供版本化接口、稳定错误结构和连接后端的板位操作页，固定前端依赖并支持可重复构建。

## 领域规则与不变量

1. 孔位由板号、行号和列号唯一确定；锁定布局后不得改动。一个样本管只能占用一个孔位，每个样本必须恰有规则指定数量的复孔，对照数量和位置必须满足规则。
2. 荧光值、阈值和阈值循环数均使用统一缩放整数；运算采用二分之一远离零舍入，拒绝负循环号、除零、缩放不一致和 int64 溢出。
3. 曲线分片必须从序号 1 形成连续前缀，循环区间不得重叠。相同操作号与内容返回原结果；内容变化返回冲突且不改变游标。
4. 只有完整曲线可判读。越阈必须连续达到锁定窗口长度；阈值循环数仅在首次跨越阈值的相邻两点间线性插值，边界值视为命中。
5. 阳性对照须在规定阈值循环数范围内，阴性对照不得命中。任一对照失效时，相关样本不能获得合格结论。
6. 污染闭包只能沿锁定边传播，输出按板号、行、列排序；创建复验须比较当前代次与缺陷摘要，并发请求至多产生一个新代次。
7. 旧代次证据只追加且不可覆盖；代次切换后到达的旧曲线、复核和裁定请求不得影响当前投影。
8. 放行要求当前代次全部必需孔位判读完成、对照有效、复孔一致、污染闭包已复验，并由两名不同且合格的人员复核相同证据摘要。

## 数据模型与持久化

1. ProtocolSnapshot：靶标、缩放因子、基线区间、连续越阈窗口、对照范围、复孔规则、传播边和规则摘要。
2. AssayBatch/Plate/Well：批次状态、当前代次、板布局；孔位类型、样本管码、提取批次、移液批次和试剂批号。
3. InstrumentRun/CurveChunk：运行号、孔位、代次、分片序号、循环范围、整数荧光数组、内容摘要、上传游标和完成标记。
4. Interpretation：基线值、首次越阈区间、缩放阈值循环数、阳性状态、对照结论、复孔结论和证据摘要。
5. ContaminationSet/Generation：种子、传播边、排序闭包、来源摘要、新代次和旧证据引用。
6. Review/FinalDecision/EventLog/Snapshot：复核身份与资格、终局类型及序号、事务事件、校验和、提交标记、幂等响应和恢复快照。

## 公开接口

1. POST /api/v1/protocols 与 POST /api/v1/batches/{id}/lock：创建规则并锁定板位，返回规则摘要、孔位清单和稳定校验原因。
2. POST /api/v1/batches/{id}/loads：装载样本管；请求携带 Operation-Id，相同内容重试返回原事务号。
3. POST /api/v1/runs 与 POST /api/v1/runs/{id}/chunks：创建仪器运行并上传曲线分片；GET /cursor 返回确定性连续游标。
4. POST /api/v1/batches/{id}/interpret：判读完整曲线；GET /wells 按板、行、列返回孔位证据。
5. POST /api/v1/batches/{id}/contamination/evaluate 与 /retests：计算污染闭包并以比较交换创建复验代次。
6. POST /api/v1/batches/{id}/reviews 与 /final-decisions：提交复核并竞争终局；GET /api/v1/batches/{id} 返回当前投影。所有错误采用 {code,message,reasons[]} 且原因稳定排序。
7. GET / 提供可构建前端，支持规则锁定、板位装载、曲线上传、判读、复验和终局操作，所有状态均来自上述接口。

## 失败边界

1. 稳定错误码覆盖布局缺失、管码重复、对照不足、规则摘要陈旧、分片缺口、区间重叠、幂等冲突、定点溢出、曲线未完成、代次过期、复验竞争、复核人重复和终局已存在。
2. 装载、分片接收、判读、复验创建与终局写入仅在提交标记持久化后发布；故障注入点覆盖日志写入前、写入中和提交前，失败事务不得留下部分状态。
3. 日志记录带长度和校验和；未提交尾部可忽略，已提交记录损坏必须阻止启动并报告明确恢复错误。
4. 批次、运行和终局采用固定锁顺序；同步并发下，分片冲突、复验创建和终局竞争均产生唯一确定赢家。
5. 服务不依赖真实仪器、系统时间或随机数；公共测试使用脚本化曲线、显式逻辑序号和同步屏障。
6. JSON 解析失败不得调用领域写操作；响应数组、原因、闭包、事件和凭据均采用稳定排序或内容寻址。

## 验收标准

1. 锁定操作固定规则、96 孔布局、样本复孔、三类对照、批号及传播边；缺孔、重复管码、对照不足和陈旧摘要以稳定错误拒绝。
2. 曲线分片只能形成连续无重叠前缀；相同请求安全重试，冲突内容、缺片、错误代次及故障事务不改变上传游标。
3. 定点判读在阈值边界、连续窗口、插值半值、除零和溢出输入上产生精确可断言结果。
4. 对照失效和复孔不一致会阻止合格结论；污染种子沿锁定边生成排序且最小的闭包。
5. 并发复验只能创建一个新代次，旧证据保持可读，旧代次迟到数据不会改变当前结论。
6. 两名不同合格人员复核同一证据摘要后方可裁定；并发裁定只持久化一个终局，重启后序号和结果不变。
7. 真实持久层可从快照和事件日志恢复布局、游标、判读、闭包、代次、幂等响应与终局，并能识别已提交记录损坏。
8. 仓库含 20—28 个生产 Go 文件、至少 5 个有意义的包和 2000—3000 行有效生产 Go 代码，目标约 2500 行；至少 4 个公开测试文件包含不少于 12 个公开测试，前端固定依赖并可在 amd64 与 arm64 容器中构建运行。

## 确定性测试场景

1. 板位锁定：验证完整布局成功，以及缺失复孔、重复管码、对照不足、非法传播边和陈旧摘要。
2. 分片前缀：依次上传三片后完成曲线；验证跳号、重叠、同操作号异内容和旧代次均不改变游标。
3. 定点判读：用固定整数曲线覆盖恰好越阈、连续窗口不足、半值舍入、除零和乘法溢出。
4. 对照门控：分别构造阳性对照过早、过晚、未命中及阴性对照命中，断言相关样本无法合格。
5. 污染闭包：给定固定孔位、移液批次和提取批次边，精确断言多种子闭包内容与排序。
6. 复验竞争：同步提交两个相同复验请求，断言唯一新代次；不同缺陷摘要的失败请求不修改状态。
7. 代次隔离：创建复验后提交旧代次曲线和复核，断言旧事件可查询但当前投影不变。
8. 事务恢复：在分片、判读和终局提交前注入故障，重启后断言无部分记录；成功事务完整恢复。
9. 日志损坏：破坏未提交尾部时可恢复，破坏已提交记录时启动返回确定恢复错误。
10. 复核规则：同一人员重复、资格不足和证据摘要过期均失败，两名有效人员成功。
11. 终局竞争：同步提交三种裁定，断言仅一个成功且多次重启后结果一致。
12. HTTP 联通：使用 httptest 完成锁定至裁定流程，验证错误原因排序、前端资源加载及页面操作调用真实接口。

## 组件追踪关系

1. 实验规则与板位锁定支撑布局、复孔、对照、批号和传播边锁定，并覆盖板位锁定测试。
2. 样本装载与曲线接收支撑唯一装载、连续分片、幂等游标和冲突检测，并覆盖分片与事务恢复测试。
3. 定点判读与质量门控支撑越阈插值、对照有效性和复孔一致性，并覆盖定点与对照测试。
4. 污染闭包与复验代次支撑最小传播集合、并发比较交换及旧代次隔离，并覆盖闭包和复验竞争测试。
5. 事件存储与终局仲裁支撑日志校验、快照恢复、双人复核和唯一终局，并覆盖损坏恢复、复核和终局竞争测试。
6. HTTP API 与嵌入式前端支撑完整操作流程、稳定错误和真实后端联通，并覆盖 HTTP 联通测试；建议包布局为 cmd/server、internal/protocol、internal/assay、internal/analysis、internal/verdict、internal/store 和 internal/httpapi。

## 独特性

项目以扩增板孔位、整数荧光曲线、对照门控和实验污染传播图为统一业务核心，将连续分片证据、确定性阈值插值、复验代次隔离与单写批次裁定组合为可恢复的实验质量闭环。
